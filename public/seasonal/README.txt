Place your seasonal images here:

  left.png   — left side image (slides in from left)
  right.png  — right side image (slides in from right)

Recommended: 300×400 px, PNG with transparent background.

Then in SeasonalBanner.jsx update:
  const LEFT_IMAGE  = '/seasonal/left.png';
  const RIGHT_IMAGE = '/seasonal/right.png';
